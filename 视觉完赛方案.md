# 视觉系统完赛方案 — 从现状到比赛的完整路径

## 一、现状评估

### 已完成 ✅
| 模块 | 状态 | 说明 |
|------|------|------|
| 摄像头采集 | ✅ | USB摄像头 640x480@30fps, 支持热插拔自动探测 |
| 颜色检测 | ✅ | HSV分割蓝/黄/白, 高斯降噪, 形态学过滤 |
| 帧间跟踪 | ✅ | Tracker平滑, 消除抖动 |
| 目标分类 | ✅ | 敌方/友方/中立分类, 优先级决策 |
| 调试流媒体 | ✅ | MJPEG HTTP流, SSH隧道远程查看 |
| 标定工具 | ✅ | calibrate.py 支持无头/GUI两种模式 |
| UART协议 | ✅ 定义 | 协议格式已定, 代码已写, **未实际联调** |

### 未完成 ❌
| 模块 | 重要度 | 说明 |
|------|--------|------|
| STM32 串口联调 | **P0 致命** | 视觉结果发不到下位机=视觉白做 |
| 开机自启动 | **P0 致命** | 比赛不能SSH进去手动启动程序 |
| 线上赛炸弹块规避 | **P1 高** | 推下炸弹块 -6分, 需识别AprilTag或特定颜色 |
| 赛场光照适配 | **P1 高** | HSV阈值受光照影响大, 需赛前快速标定流程 |
| 敌方机器人检测 | P2 中 | 可选: 用YOLO或色差检测对手, 非必须(STM32红外测距可替代) |
| 性能监控/日志 | P2 中 | 比赛后复盘需要日志 |

---

## 二、完赛最小可行方案 (MVP)

### 核心原则
> 只做比赛**必须**的功能, 不做锦上添花。宁可功能少但稳定, 不要功能全但崩溃。

### 比赛流程中视觉系统的角色

```
开机 → 自启动vision → 等待STM32发送己方颜色('b'/'y') → 进入检测循环
                                                          ↓
                              ┌─────────────────────────────────────────┐
                              │  每帧处理 (~30ms, 30fps)                │
                              │  1. 采集摄像头画面                       │
                              │  2. HSV颜色分割(蓝/黄/白)               │
                              │  3. 轮廓提取 + 过滤 + 跟踪平滑           │
                              │  4. 敌我分类(根据STM32告知的己方颜色)      │
                              │  5. 选出最优先目标                       │
                              │  6. UART发送: $E,cx,cy,area,dir*CS\n    │
                              └─────────────────────────────────────────┘
                                                          ↓
                              STM32 收到 → 融合红外/灰度 → 执行攻击/推块策略
```

---

## 三、开发任务清单 (按优先级)

### P0: 必须完成 — 不做就无法比赛

#### Task 1: STM32 UART 联调
**目标**: 视觉结果能被STM32正确解析并响应

**工作内容**:
1. **硬件连线**: LubanCat `/dev/ttyS10` TX/RX → STM32 USART2 PD6/PD5 (交叉连接, 共地)
2. **STM32端解析代码**: 在 `Core/Src/` 中新增 `vision_parser.c`
   - DMA接收UART数据, 解析 `$type,cx,cy,area,dir*CS\n` 格式
   - 校验CS, 提取目标类型/坐标/方向
   - 写入全局变量供 DecisionTask 读取
3. **LubanCat端**: comm.py 已就绪, 需确认波特率/电平匹配
4. **联调测试**:
   - 视觉发数据 → STM32串口打印确认收到
   - STM32发'b'/'y' → 视觉切换颜色确认

**验收标准**: 摄像头对着蓝色物体 → STM32串口输出显示 `E,320,240,xxx`

#### Task 2: 开机自启动 (systemd)
**目标**: LubanCat上电后自动运行视觉程序, 无需人工干预

**工作内容**:
1. 创建 systemd service 文件 `/etc/systemd/system/robot-vision.service`
2. 配置:
   - `After=network.target` 网络就绪后启动
   - `Restart=always` 崩溃自动重启
   - `RestartSec=3` 重启间隔3秒
   - 使用系统Python (`/usr/bin/python3`)
   - 工作目录 `/home/cat/vision/`
3. 日志输出到 `/var/log/robot-vision.log`
4. 测试: 重启板卡, 确认视觉自动运行

**验收标准**: 拔电→重新上电→30秒内视觉开始发送UART数据

#### Task 3: 赛前快速标定流程
**目标**: 到赛场后5分钟内完成颜色标定

**工作内容**:
1. 优化 `calibrate.py --headless` 模式:
   - SSH连接板卡
   - 依次对准蓝/黄/白能量块, 按回车采样
   - 自动计算HSV范围, 保存到 `hsv_calibration.json`
2. `main.py` 启动时自动加载 `hsv_calibration.json` (如存在则覆盖config默认值)
3. 编写标定操作手册 (一页纸, 给队友看)

**验收标准**: 在不同光照下, 标定后能准确识别三种颜色能量块

---

### P1: 应该完成 — 不做会明显丢分

#### Task 4: 线上赛炸弹块规避
**目标**: 识别炸弹块, 发送警告让STM32躲避 (推下炸弹块 -6分!)

**分析**:
- 线上赛的炸弹块外观需要确认(可能有特定颜色/标记)
- 如果炸弹块有独特颜色: 加一个HSV通道即可
- 如果需要AprilTag区分: 安装 `pupil-apriltags` 库

**工作内容**:
1. 确认线上赛炸弹块的视觉特征
2. 在detector中新增炸弹块检测
3. UART协议新增 `B` 类型(炸弹), STM32收到后执行回避

#### Task 5: 光照鲁棒性增强
**目标**: 减少赛场灯光变化对检测的影响

**工作内容**:
1. 自动白平衡: 摄像头设置固定白平衡(避免自动调整导致色偏)
2. HSV阈值自适应: 基于标定数据动态微调
3. 增加亮度归一化预处理 (CLAHE均衡化)

---

### P2: 可以完成 — 做了有加分

#### Task 6: YOLO目标检测 (NPU加速)
**目标**: 用RKNN NPU跑YOLOv5检测敌方机器人

**工作内容**:
1. 在ultralytics conda环境中用rknnlite加载yolov5s.rknn
2. 双通道并行: 颜色检测(CPU) + YOLO(NPU)
3. YOLO检测结果补充到目标列表

#### Task 7: 比赛日志与复盘
**目标**: 记录每场比赛的视觉数据, 赛后分析

**工作内容**:
1. 每场比赛录制关键帧到SD卡
2. 记录每帧的检测结果、UART发送内容
3. 赛后可回放分析

---

## 四、时间规划

假设距离比赛还有 **2周**:

| 阶段 | 天数 | 任务 | 交付物 |
|------|------|------|--------|
| **Day 1-2** | 2天 | Task 1: UART联调 | 视觉↔STM32双向通信OK |
| **Day 3** | 1天 | Task 2: 开机自启动 | 上电自动运行 |
| **Day 3** | 同上 | Task 3: 标定流程 | 标定手册+自动加载 |
| **Day 4-5** | 2天 | 整车联调: 视觉+运动+策略 | 机器人能追踪能量块 |
| **Day 6-7** | 2天 | Task 4: 炸弹块规避 + Task 5: 光照鲁棒 | 线上赛安全 |
| **Day 8-10** | 3天 | 全系统对抗测试 | 模拟比赛环境反复测试 |
| **Day 11-12** | 2天 | Bug修复 + 参数微调 | 稳定版本 |
| **Day 13-14** | 2天 | 赛前演练 + 备用方案 | 比赛就绪 |

---

## 五、STM32端需要的代码变更

STM32 需在现有固件中新增以下内容:

### 5.1 vision_parser.h
```c
typedef struct {
    char type;        // 'E'敌方 'N'中立 'F'友方 'X'无目标 'B'炸弹
    int16_t cx;       // 目标中心x (0-640)
    int16_t cy;       // 目标中心y (0-480)
    int32_t area;     // 目标面积
    int8_t  dir;      // 方向 [-100,+100] 负=左 正=右
    uint8_t valid;    // 数据是否有效
} VisionTarget_t;

extern VisionTarget_t vision_target;
void Vision_ParseUART(uint8_t *buf, uint16_t len);
```

### 5.2 在 DecisionTask 中使用视觉数据
```c
// 在 robot_fight.c 或 robot_roaming.c 中
if (vision_target.valid && vision_target.type == 'E') {
    // 有敌方能量块
    if (vision_target.dir > 20) {
        // 目标在右边, 右转
        Motor_TurnRight(SPEED_TURN_MEDIUM);
    } else if (vision_target.dir < -20) {
        // 目标在左边, 左转
        Motor_TurnLeft(SPEED_TURN_MEDIUM);
    } else {
        // 目标在中间, 直行推
        Motor_Forward(SPEED_HIGH);
    }
}
```

---

## 六、风险与备用方案

| 风险 | 影响 | 备用方案 |
|------|------|---------|
| 赛场光照极端(强背光/暗光) | 颜色检测全面失效 | 切纯红外模式, 不靠视觉, STM32自主巡逻 |
| 摄像头比赛中松动/故障 | 无视觉 | STM32独立运行漫游攻击模式(已有robot_roaming.c) |
| UART通信丢包/延迟 | 视觉数据不及时 | 加重发机制; STM32设超时: 200ms无数据则切自主模式 |
| LubanCat死机 | 视觉完全失效 | systemd自动重启; STM32看门狗检测通信断开 |

---

## 七、赛前检查清单

- [ ] 标定完成: 蓝/黄/白三色在赛场光照下检测正确
- [ ] UART通信: 视觉发数据 → STM32正确解析并响应
- [ ] 开机自启: 断电重启后30秒内视觉自动运行
- [ ] 备用方案: STM32独立模式(无视觉)能正常运动
- [ ] 电池充满, 备用电池就绪
- [ ] 摄像头固定牢固, USB线缆扎紧
- [ ] config.py 中的阈值为赛场标定值(非默认值)
